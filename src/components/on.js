import { Router } from "../router";

Router.register("/on", (mount) => {
  mount.innerHTML = `
        <section class="hero">
            <h1>on()</h1>
            <p>This method is not yet implemented.</p>
        </section>
    `;
});
