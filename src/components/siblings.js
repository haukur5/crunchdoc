import { Router } from "../router";

Router.register("/siblings", (mount) => {
  mount.innerHTML = `
        <section class="hero">
            <h1>siblings()</h1>
            <p>This method is not yet implemented.</p>
        </section>
    `;
});
