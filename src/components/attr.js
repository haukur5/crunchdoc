import { Router } from "../router";

Router.register("/attr", (mount) => {
  mount.innerHTML = `
        <section class="hero">
            <h1>attr()</h1>
            <p>This method is not yet implemented.</p>
        </section>
    `;
});
