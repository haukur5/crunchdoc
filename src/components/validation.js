import { Router } from "../router";

Router.register("/validation", (mount) => {
  mount.innerHTML = `
        <section class="hero">
            <h1>validation()</h1>
            <p>This method is not yet implemented.</p>
        </section>
    `;
});
